---
deployment:
  tasks:
    - export DEPLOYPATH=/home/marquito/11once.cl/web
    - mkdir -p $DEPLOYPATH
    - /bin/cp index.html $DEPLOYPATH/
    - /bin/cp -R assets $DEPLOYPATH/
    - /bin/cp .htaccess favicon.ico robots.txt placeholder.svg $DEPLOYPATH/
    - /bin/cp -R correos $DEPLOYPATH/
